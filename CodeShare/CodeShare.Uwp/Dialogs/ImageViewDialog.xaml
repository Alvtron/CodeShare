<ContentDialog
    x:Class="CodeShare.Uwp.Dialogs.ImageViewDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodeShare.Uwp.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:converters="using:CodeShare.Uwp.Converters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ContentDialog.Resources>
        <converters:ImageConverter x:Key="ImageConverter" />
        <converters:VisibilityConverter x:Key="VisibillityConverter" />
    </ContentDialog.Resources>
    
    <Grid Width="auto" Height="auto">
        <Image Source="{x:Bind Image, Mode=OneWay, Converter={StaticResource ImageConverter}}" Stretch="UniformToFill" Width="auto" Height="auto" MinHeight="200" MinWidth="200" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        <TextBlock Text="{x:Bind Image.Description, Mode=OneWay}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="48" />
    </Grid>
</ContentDialog>

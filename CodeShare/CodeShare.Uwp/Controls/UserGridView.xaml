<UserControl
    x:Class="CodeShare.Uwp.Controls.UserGridView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodeShare.Uwp.Controls"
    xmlns:controls="using:CodeShare.Uwp.Controls"
    xmlns:model="using:CodeShare.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <GridView
            Header="{x:Bind Header, Mode=OneWay}"
            ItemsSource="{x:Bind Users, Mode=OneWay}"
            ItemClick="GridView_ItemClick"
            IsItemClickEnabled="True">
        <GridView.ItemTemplate>
            <DataTemplate x:DataType="model:User">
                <controls:ProfilePictureItem User="{Binding}" Orientation="Vertical" Size="50" Margin="12" />
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</UserControl>

<UserControl
    x:Class="CodeShare.Uwp.Controls.QuestionItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodeShare.Uwp.Controls"
    xmlns:converters="using:CodeShare.Uwp.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <converters:ImageConverter x:Key="ImageConverter" />
        <converters:ColorConverter x:Key="ColorConverter" />
    </UserControl.Resources>

    <Grid Width="auto" Height="auto" Background="{x:Bind Question.User.Color, Converter={StaticResource ColorConverter}}" Padding="12">
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center">
            <TextBlock Text="{x:Bind Question.Title, Mode=OneWay}" />
            <TextBlock Foreground="DimGray">
                <Run Text="{x:Bind Question.CodeLanguage.Name, Mode=OneWay}" />
                <Run Text="•" />
                <Run Text="{x:Bind Question.User.Name, Mode=OneWay}" />
                <Run Text="•" />
                <Run Text="{x:Bind Question.Views, Mode=OneWay}" />
                <Run Text="views" />
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>

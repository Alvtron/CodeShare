<UserControl
    x:Class="CodeShare.Uwp.Controls.QuestionItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodeShare.Uwp.Controls"
    xmlns:converters="using:CodeShare.Uwp.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <converters:ImageConverter x:Key="ImageConverter" />
        <converters:ColorConverter x:Key="ColorConverter" />
    </UserControl.Resources>

    <Grid Width="200" Height="100">
        <Image Source="{x:Bind Question.User.ProfilePicture.Url, Mode=OneWay, Converter={StaticResource ImageConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="12">
            <TextBlock Text="{x:Bind Question.Title, Mode=OneWay}" />
            <TextBlock Text="{x:Bind Question.User.Name, Mode=OneWay}" Foreground="DimGray" />
        </StackPanel>
    </Grid>
</UserControl>
